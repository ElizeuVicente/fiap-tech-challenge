apiVersion: v1
kind: Service
metadata:
  name: oficina-api
  namespace: oficina
spec:
  selector:
    app: oficina-api
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
